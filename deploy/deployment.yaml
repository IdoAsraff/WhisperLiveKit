apiVersion: apps/v1
kind: Deployment
metadata:
  name: wlk-qwen
  namespace: atero
  labels:
    app: wlk-qwen
spec:
  replicas: 8
  selector:
    matchLabels:
      app: wlk-qwen
  template:
    metadata:
      labels:
        app: wlk-qwen
    spec:
      containers:
      - name: wlk
        image: registry.gitlab.com/crusoeenergy/atero/atero-um/wlk-qwen3:v2
        env:
        - name: WLK_USE_VLLM
          value: "true"
        ports:
        - containerPort: 8000
          name: http
        resources:
          requests:
            cpu: "4"
            nvidia.com/gpu: "1"
          limits:
            nvidia.com/gpu: "1"
        readinessProbe:
          httpGet:
            path: /
            port: 8000
          initialDelaySeconds: 240
          periodSeconds: 10
          timeoutSeconds: 5
        livenessProbe:
          httpGet:
            path: /
            port: 8000
          initialDelaySeconds: 300
          periodSeconds: 30
          timeoutSeconds: 5
      imagePullSecrets:
      - name: gitlab-registry
---
apiVersion: v1
kind: Service
metadata:
  name: wlk-qwen
  namespace: atero
  labels:
    app: wlk-qwen
spec:
  selector:
    app: wlk-qwen
  ports:
  - port: 8000
    targetPort: 8000
    name: http
